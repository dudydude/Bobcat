<h1>Events</h1>

<ul>
  <% events.forEach((event) => { %>
    <li>
    <%= event.name %> - <%= event.venue %> - <%= event.date %>
    <a href="/events/<%= event._id %>">View Details</a>
    </li>
  <% }) %>
</ul>
